services:
  9router:
    build: .
    image: 9router:local
    container_name: 9router
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - 9router_data:/root/.9router
    restart: unless-stopped

volumes:
  9router_data:
